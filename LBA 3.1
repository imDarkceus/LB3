#include <stdio.h>
main(){
const int u = 5;
const float a = 9.81;
float t;
printf("Enter the value of time(t): ");
scanf("%f",&t);
float v ;
v = (float)u+a*t;
printf("The Velocity is: %.2f",(float)v);
}
